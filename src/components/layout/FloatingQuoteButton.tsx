// Moved from components/FloatingQuoteButton.tsx
import React from 'react';

const FloatingQuoteButton = () => {
  return (
    <button>
      {/* Floating Quote Button content */}
    </button>
  );
};

export default FloatingQuoteButton;